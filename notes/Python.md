## Python {docsify-ignore}

- 更换pip源
  - 临时配置：`pip(pip3) install [package-name] -i https://pypi.tuna.tsinghua.edu.cn/simple/`
  - 设为默认配置：`pip(pip3) install -U pip` `pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple/`

---
